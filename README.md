# Watchdog
WatchDog is a hybrid infrastructure monitoring system with:  
- Hub: Central cloud server (REST API, WebSockets, Dashboard) 
- Agent: Lightweight Go binary deployed inside customer networks
